// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

export class PersonalNameserver extends pulumi.CustomResource {
    /**
     * Get an existing PersonalNameserver resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: PersonalNameserverState, opts?: pulumi.CustomResourceOptions): PersonalNameserver {
        return new PersonalNameserver(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'namecheap:index/personalNameserver:PersonalNameserver';

    /**
     * Returns true if the given object is an instance of PersonalNameserver.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is PersonalNameserver {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === PersonalNameserver.__pulumiType;
    }

    /**
     * The registered domain the personal nameserver belongs to (e.g. "example.com"). Must be a root domain present on the account, not a subdomain.
     */
    declare public readonly domain: pulumi.Output<string>;
    /**
     * The IP address the personal nameserver resolves to (the glue record's address).
     */
    declare public readonly ip: pulumi.Output<string>;
    /**
     * The fully qualified hostname of the personal nameserver to register (e.g. "ns1.example.com").
     */
    declare public readonly nameserver: pulumi.Output<string>;
    declare public readonly personalNameserverId: pulumi.Output<string>;

    /**
     * Create a PersonalNameserver resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: PersonalNameserverArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: PersonalNameserverArgs | PersonalNameserverState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as PersonalNameserverState | undefined;
            resourceInputs["domain"] = state?.domain;
            resourceInputs["ip"] = state?.ip;
            resourceInputs["nameserver"] = state?.nameserver;
            resourceInputs["personalNameserverId"] = state?.personalNameserverId;
        } else {
            const args = argsOrState as PersonalNameserverArgs | undefined;
            if (args?.domain === undefined && !opts.urn) {
                throw new Error("Missing required property 'domain'");
            }
            if (args?.ip === undefined && !opts.urn) {
                throw new Error("Missing required property 'ip'");
            }
            if (args?.nameserver === undefined && !opts.urn) {
                throw new Error("Missing required property 'nameserver'");
            }
            resourceInputs["domain"] = args?.domain;
            resourceInputs["ip"] = args?.ip;
            resourceInputs["nameserver"] = args?.nameserver;
            resourceInputs["personalNameserverId"] = args?.personalNameserverId;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(PersonalNameserver.__pulumiType, name, resourceInputs, opts, false /*dependency*/, utilities.getPackage());
    }
}

/**
 * Input properties used for looking up and filtering PersonalNameserver resources.
 */
export interface PersonalNameserverState {
    /**
     * The registered domain the personal nameserver belongs to (e.g. "example.com"). Must be a root domain present on the account, not a subdomain.
     */
    domain?: pulumi.Input<string | undefined>;
    /**
     * The IP address the personal nameserver resolves to (the glue record's address).
     */
    ip?: pulumi.Input<string | undefined>;
    /**
     * The fully qualified hostname of the personal nameserver to register (e.g. "ns1.example.com").
     */
    nameserver?: pulumi.Input<string | undefined>;
    personalNameserverId?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a PersonalNameserver resource.
 */
export interface PersonalNameserverArgs {
    /**
     * The registered domain the personal nameserver belongs to (e.g. "example.com"). Must be a root domain present on the account, not a subdomain.
     */
    domain: pulumi.Input<string>;
    /**
     * The IP address the personal nameserver resolves to (the glue record's address).
     */
    ip: pulumi.Input<string>;
    /**
     * The fully qualified hostname of the personal nameserver to register (e.g. "ns1.example.com").
     */
    nameserver: pulumi.Input<string>;
    personalNameserverId?: pulumi.Input<string | undefined>;
}
