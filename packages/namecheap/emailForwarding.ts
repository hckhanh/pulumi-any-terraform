// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

export class EmailForwarding extends pulumi.CustomResource {
    /**
     * Get an existing EmailForwarding resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: EmailForwardingState, opts?: pulumi.CustomResourceOptions): EmailForwarding {
        return new EmailForwarding(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'namecheap:index/emailForwarding:EmailForwarding';

    /**
     * Returns true if the given object is an instance of EmailForwarding.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is EmailForwarding {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === EmailForwarding.__pulumiType;
    }

    /**
     * The registered root domain whose email forwarding is managed (e.g. "example.com"). Must be a root domain, not a subdomain.
     */
    declare public readonly domain: pulumi.Output<string>;
    declare public readonly emailForwardingId: pulumi.Output<string>;
    /**
     * Map of mailbox alias to destination email address, e.g. { info = "me@example.com" }. Use "*" as the alias for a catch-all. This resource owns the domain's entire forwarding table: it must be non-empty (destroy the resource to clear all forwarding) and any rule not listed here is removed on the next apply.
     */
    declare public readonly forwards: pulumi.Output<{[key: string]: string}>;

    /**
     * Create a EmailForwarding resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: EmailForwardingArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: EmailForwardingArgs | EmailForwardingState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as EmailForwardingState | undefined;
            resourceInputs["domain"] = state?.domain;
            resourceInputs["emailForwardingId"] = state?.emailForwardingId;
            resourceInputs["forwards"] = state?.forwards;
        } else {
            const args = argsOrState as EmailForwardingArgs | undefined;
            if (args?.domain === undefined && !opts.urn) {
                throw new Error("Missing required property 'domain'");
            }
            if (args?.forwards === undefined && !opts.urn) {
                throw new Error("Missing required property 'forwards'");
            }
            resourceInputs["domain"] = args?.domain;
            resourceInputs["emailForwardingId"] = args?.emailForwardingId;
            resourceInputs["forwards"] = args?.forwards;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(EmailForwarding.__pulumiType, name, resourceInputs, opts, false /*dependency*/, utilities.getPackage());
    }
}

/**
 * Input properties used for looking up and filtering EmailForwarding resources.
 */
export interface EmailForwardingState {
    /**
     * The registered root domain whose email forwarding is managed (e.g. "example.com"). Must be a root domain, not a subdomain.
     */
    domain?: pulumi.Input<string | undefined>;
    emailForwardingId?: pulumi.Input<string | undefined>;
    /**
     * Map of mailbox alias to destination email address, e.g. { info = "me@example.com" }. Use "*" as the alias for a catch-all. This resource owns the domain's entire forwarding table: it must be non-empty (destroy the resource to clear all forwarding) and any rule not listed here is removed on the next apply.
     */
    forwards?: pulumi.Input<{[key: string]: pulumi.Input<string>} | undefined>;
}

/**
 * The set of arguments for constructing a EmailForwarding resource.
 */
export interface EmailForwardingArgs {
    /**
     * The registered root domain whose email forwarding is managed (e.g. "example.com"). Must be a root domain, not a subdomain.
     */
    domain: pulumi.Input<string>;
    emailForwardingId?: pulumi.Input<string | undefined>;
    /**
     * Map of mailbox alias to destination email address, e.g. { info = "me@example.com" }. Use "*" as the alias for a catch-all. This resource owns the domain's entire forwarding table: it must be non-empty (destroy the resource to clear all forwarding) and any rule not listed here is removed on the next apply.
     */
    forwards: pulumi.Input<{[key: string]: pulumi.Input<string>}>;
}
