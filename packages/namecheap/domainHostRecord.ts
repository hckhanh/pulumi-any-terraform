// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

export class DomainHostRecord extends pulumi.CustomResource {
    /**
     * Get an existing DomainHostRecord resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: DomainHostRecordState, opts?: pulumi.CustomResourceOptions): DomainHostRecord {
        return new DomainHostRecord(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'namecheap:index/domainHostRecord:DomainHostRecord';

    /**
     * Returns true if the given object is an instance of DomainHostRecord.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is DomainHostRecord {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === DomainHostRecord.__pulumiType;
    }

    /**
     * The record's value, whose meaning depends on <span pulumi-lang-nodejs="`type`" pulumi-lang-dotnet="`Type`" pulumi-lang-go="`type`" pulumi-lang-python="`type`" pulumi-lang-yaml="`type`" pulumi-lang-java="`type`" pulumi-lang-hcl="`type`">`type`</span>: an IP address for A/AAAA, a hostname for CNAME/MX/NS, arbitrary text for TXT, a URL for URL/URL301/FRAME. Changing this does not force a new resource: the existing record is edited, in one write that swaps the value, so the name never stops resolving. The resource id is re-rendered, since the address is part of it.
     */
    declare public readonly address: pulumi.Output<string>;
    /**
     * The registered root domain the record belongs to (e.g. `example.com`). Must be a root domain on the account, not a subdomain. Changing this forces a new resource.
     */
    declare public readonly domain: pulumi.Output<string>;
    declare public readonly domainHostRecordId: pulumi.Output<string>;
    /**
     * The sub-domain the record answers for, or `@` for the domain itself (e.g. <span pulumi-lang-nodejs="`www`" pulumi-lang-dotnet="`Www`" pulumi-lang-go="`www`" pulumi-lang-python="`www`" pulumi-lang-yaml="`www`" pulumi-lang-java="`www`" pulumi-lang-hcl="`www`">`www`</span>). Changing this forces a new resource, because the host is part of the record's identity.
     */
    declare public readonly hostname: pulumi.Output<string>;
    /**
     * The MX preference, lower being preferred, between 0 and 255. Applies to MX records only — Namecheap stores a fixed 10 for every other type, so the value is ignored there rather than fought over. For MX records it is part of what identifies the record, so a primary and a backup mail server can name the same host.
     */
    declare public readonly mxPref: pulumi.Output<number | undefined>;
    /**
     * Time to live in seconds, between 60 and 60000. Changing this edits the existing record rather than forcing a new resource.
     */
    declare public readonly ttl: pulumi.Output<number | undefined>;
    /**
     * The record type: A, AAAA, ALIAS, CAA, CNAME, MX, MXE, NS, TXT, URL, URL301, FRAME. Changing this forces a new resource. MX and MXE records additionally require the domain's email routing to be set accordingly — see the resource documentation.
     */
    declare public readonly type: pulumi.Output<string>;

    /**
     * Create a DomainHostRecord resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: DomainHostRecordArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: DomainHostRecordArgs | DomainHostRecordState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as DomainHostRecordState | undefined;
            resourceInputs["address"] = state?.address;
            resourceInputs["domain"] = state?.domain;
            resourceInputs["domainHostRecordId"] = state?.domainHostRecordId;
            resourceInputs["hostname"] = state?.hostname;
            resourceInputs["mxPref"] = state?.mxPref;
            resourceInputs["ttl"] = state?.ttl;
            resourceInputs["type"] = state?.type;
        } else {
            const args = argsOrState as DomainHostRecordArgs | undefined;
            if (args?.address === undefined && !opts.urn) {
                throw new Error("Missing required property 'address'");
            }
            if (args?.domain === undefined && !opts.urn) {
                throw new Error("Missing required property 'domain'");
            }
            if (args?.hostname === undefined && !opts.urn) {
                throw new Error("Missing required property 'hostname'");
            }
            if (args?.type === undefined && !opts.urn) {
                throw new Error("Missing required property 'type'");
            }
            resourceInputs["address"] = args?.address;
            resourceInputs["domain"] = args?.domain;
            resourceInputs["domainHostRecordId"] = args?.domainHostRecordId;
            resourceInputs["hostname"] = args?.hostname;
            resourceInputs["mxPref"] = args?.mxPref;
            resourceInputs["ttl"] = args?.ttl;
            resourceInputs["type"] = args?.type;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(DomainHostRecord.__pulumiType, name, resourceInputs, opts, false /*dependency*/, utilities.getPackage());
    }
}

/**
 * Input properties used for looking up and filtering DomainHostRecord resources.
 */
export interface DomainHostRecordState {
    /**
     * The record's value, whose meaning depends on <span pulumi-lang-nodejs="`type`" pulumi-lang-dotnet="`Type`" pulumi-lang-go="`type`" pulumi-lang-python="`type`" pulumi-lang-yaml="`type`" pulumi-lang-java="`type`" pulumi-lang-hcl="`type`">`type`</span>: an IP address for A/AAAA, a hostname for CNAME/MX/NS, arbitrary text for TXT, a URL for URL/URL301/FRAME. Changing this does not force a new resource: the existing record is edited, in one write that swaps the value, so the name never stops resolving. The resource id is re-rendered, since the address is part of it.
     */
    address?: pulumi.Input<string | undefined>;
    /**
     * The registered root domain the record belongs to (e.g. `example.com`). Must be a root domain on the account, not a subdomain. Changing this forces a new resource.
     */
    domain?: pulumi.Input<string | undefined>;
    domainHostRecordId?: pulumi.Input<string | undefined>;
    /**
     * The sub-domain the record answers for, or `@` for the domain itself (e.g. <span pulumi-lang-nodejs="`www`" pulumi-lang-dotnet="`Www`" pulumi-lang-go="`www`" pulumi-lang-python="`www`" pulumi-lang-yaml="`www`" pulumi-lang-java="`www`" pulumi-lang-hcl="`www`">`www`</span>). Changing this forces a new resource, because the host is part of the record's identity.
     */
    hostname?: pulumi.Input<string | undefined>;
    /**
     * The MX preference, lower being preferred, between 0 and 255. Applies to MX records only — Namecheap stores a fixed 10 for every other type, so the value is ignored there rather than fought over. For MX records it is part of what identifies the record, so a primary and a backup mail server can name the same host.
     */
    mxPref?: pulumi.Input<number | undefined>;
    /**
     * Time to live in seconds, between 60 and 60000. Changing this edits the existing record rather than forcing a new resource.
     */
    ttl?: pulumi.Input<number | undefined>;
    /**
     * The record type: A, AAAA, ALIAS, CAA, CNAME, MX, MXE, NS, TXT, URL, URL301, FRAME. Changing this forces a new resource. MX and MXE records additionally require the domain's email routing to be set accordingly — see the resource documentation.
     */
    type?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a DomainHostRecord resource.
 */
export interface DomainHostRecordArgs {
    /**
     * The record's value, whose meaning depends on <span pulumi-lang-nodejs="`type`" pulumi-lang-dotnet="`Type`" pulumi-lang-go="`type`" pulumi-lang-python="`type`" pulumi-lang-yaml="`type`" pulumi-lang-java="`type`" pulumi-lang-hcl="`type`">`type`</span>: an IP address for A/AAAA, a hostname for CNAME/MX/NS, arbitrary text for TXT, a URL for URL/URL301/FRAME. Changing this does not force a new resource: the existing record is edited, in one write that swaps the value, so the name never stops resolving. The resource id is re-rendered, since the address is part of it.
     */
    address: pulumi.Input<string>;
    /**
     * The registered root domain the record belongs to (e.g. `example.com`). Must be a root domain on the account, not a subdomain. Changing this forces a new resource.
     */
    domain: pulumi.Input<string>;
    domainHostRecordId?: pulumi.Input<string | undefined>;
    /**
     * The sub-domain the record answers for, or `@` for the domain itself (e.g. <span pulumi-lang-nodejs="`www`" pulumi-lang-dotnet="`Www`" pulumi-lang-go="`www`" pulumi-lang-python="`www`" pulumi-lang-yaml="`www`" pulumi-lang-java="`www`" pulumi-lang-hcl="`www`">`www`</span>). Changing this forces a new resource, because the host is part of the record's identity.
     */
    hostname: pulumi.Input<string>;
    /**
     * The MX preference, lower being preferred, between 0 and 255. Applies to MX records only — Namecheap stores a fixed 10 for every other type, so the value is ignored there rather than fought over. For MX records it is part of what identifies the record, so a primary and a backup mail server can name the same host.
     */
    mxPref?: pulumi.Input<number | undefined>;
    /**
     * Time to live in seconds, between 60 and 60000. Changing this edits the existing record rather than forcing a new resource.
     */
    ttl?: pulumi.Input<number | undefined>;
    /**
     * The record type: A, AAAA, ALIAS, CAA, CNAME, MX, MXE, NS, TXT, URL, URL301, FRAME. Changing this forces a new resource. MX and MXE records additionally require the domain's email routing to be set accordingly — see the resource documentation.
     */
    type: pulumi.Input<string>;
}
