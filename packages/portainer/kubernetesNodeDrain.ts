// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

export class KubernetesNodeDrain extends pulumi.CustomResource {
    /**
     * Get an existing KubernetesNodeDrain resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: KubernetesNodeDrainState, opts?: pulumi.CustomResourceOptions): KubernetesNodeDrain {
        return new KubernetesNodeDrain(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'portainer:index/kubernetesNodeDrain:KubernetesNodeDrain';

    /**
     * Returns true if the given object is an instance of KubernetesNodeDrain.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is KubernetesNodeDrain {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === KubernetesNodeDrain.__pulumiType;
    }

    /**
     * Whether to evict pods that use an emptyDir volume, whose data is lost once the pod is deleted. Mirrors `kubectl drain --delete-emptydir-data`.
     */
    declare public readonly deleteEmptyDirData: pulumi.Output<boolean | undefined>;
    /**
     * Whether to delete pods directly instead of going through the eviction API, which ignores any configured PodDisruptionBudget. Mirrors `kubectl drain --disable-eviction`.
     */
    declare public readonly disableEviction: pulumi.Output<boolean | undefined>;
    /**
     * Identifier of the Portainer Kubernetes environment the node belongs to.
     */
    declare public readonly environmentId: pulumi.Output<number>;
    /**
     * Whether to allow deletion of standalone pods that are not managed by a controller. Such pods are lost for good, so the drain refuses them unless this is enabled. Mirrors `kubectl drain --force`.
     */
    declare public readonly force: pulumi.Output<boolean | undefined>;
    /**
     * Termination grace period in seconds to apply to every evicted pod. `-1` keeps each pod's own grace period. Mirrors `kubectl drain --grace-period`.
     */
    declare public readonly gracePeriodSeconds: pulumi.Output<number | undefined>;
    /**
     * Whether to skip DaemonSet-managed pods, which would otherwise block the drain because their controller recreates them immediately. Mirrors `kubectl drain --ignore-daemonsets`.
     */
    declare public readonly ignoreDaemonSets: pulumi.Output<boolean | undefined>;
    declare public readonly kubernetesNodeDrainId: pulumi.Output<string>;
    /**
     * Name of the Kubernetes node to drain.
     */
    declare public readonly nodeName: pulumi.Output<string>;
    /**
     * Overall time in seconds to wait for the drain to complete. Mirrors `kubectl drain --timeout`.
     */
    declare public readonly timeoutSeconds: pulumi.Output<number | undefined>;

    /**
     * Create a KubernetesNodeDrain resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: KubernetesNodeDrainArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: KubernetesNodeDrainArgs | KubernetesNodeDrainState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as KubernetesNodeDrainState | undefined;
            resourceInputs["deleteEmptyDirData"] = state?.deleteEmptyDirData;
            resourceInputs["disableEviction"] = state?.disableEviction;
            resourceInputs["environmentId"] = state?.environmentId;
            resourceInputs["force"] = state?.force;
            resourceInputs["gracePeriodSeconds"] = state?.gracePeriodSeconds;
            resourceInputs["ignoreDaemonSets"] = state?.ignoreDaemonSets;
            resourceInputs["kubernetesNodeDrainId"] = state?.kubernetesNodeDrainId;
            resourceInputs["nodeName"] = state?.nodeName;
            resourceInputs["timeoutSeconds"] = state?.timeoutSeconds;
        } else {
            const args = argsOrState as KubernetesNodeDrainArgs | undefined;
            if (args?.environmentId === undefined && !opts.urn) {
                throw new Error("Missing required property 'environmentId'");
            }
            if (args?.nodeName === undefined && !opts.urn) {
                throw new Error("Missing required property 'nodeName'");
            }
            resourceInputs["deleteEmptyDirData"] = args?.deleteEmptyDirData;
            resourceInputs["disableEviction"] = args?.disableEviction;
            resourceInputs["environmentId"] = args?.environmentId;
            resourceInputs["force"] = args?.force;
            resourceInputs["gracePeriodSeconds"] = args?.gracePeriodSeconds;
            resourceInputs["ignoreDaemonSets"] = args?.ignoreDaemonSets;
            resourceInputs["kubernetesNodeDrainId"] = args?.kubernetesNodeDrainId;
            resourceInputs["nodeName"] = args?.nodeName;
            resourceInputs["timeoutSeconds"] = args?.timeoutSeconds;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(KubernetesNodeDrain.__pulumiType, name, resourceInputs, opts, false /*dependency*/, utilities.getPackage());
    }
}

/**
 * Input properties used for looking up and filtering KubernetesNodeDrain resources.
 */
export interface KubernetesNodeDrainState {
    /**
     * Whether to evict pods that use an emptyDir volume, whose data is lost once the pod is deleted. Mirrors `kubectl drain --delete-emptydir-data`.
     */
    deleteEmptyDirData?: pulumi.Input<boolean | undefined>;
    /**
     * Whether to delete pods directly instead of going through the eviction API, which ignores any configured PodDisruptionBudget. Mirrors `kubectl drain --disable-eviction`.
     */
    disableEviction?: pulumi.Input<boolean | undefined>;
    /**
     * Identifier of the Portainer Kubernetes environment the node belongs to.
     */
    environmentId?: pulumi.Input<number | undefined>;
    /**
     * Whether to allow deletion of standalone pods that are not managed by a controller. Such pods are lost for good, so the drain refuses them unless this is enabled. Mirrors `kubectl drain --force`.
     */
    force?: pulumi.Input<boolean | undefined>;
    /**
     * Termination grace period in seconds to apply to every evicted pod. `-1` keeps each pod's own grace period. Mirrors `kubectl drain --grace-period`.
     */
    gracePeriodSeconds?: pulumi.Input<number | undefined>;
    /**
     * Whether to skip DaemonSet-managed pods, which would otherwise block the drain because their controller recreates them immediately. Mirrors `kubectl drain --ignore-daemonsets`.
     */
    ignoreDaemonSets?: pulumi.Input<boolean | undefined>;
    kubernetesNodeDrainId?: pulumi.Input<string | undefined>;
    /**
     * Name of the Kubernetes node to drain.
     */
    nodeName?: pulumi.Input<string | undefined>;
    /**
     * Overall time in seconds to wait for the drain to complete. Mirrors `kubectl drain --timeout`.
     */
    timeoutSeconds?: pulumi.Input<number | undefined>;
}

/**
 * The set of arguments for constructing a KubernetesNodeDrain resource.
 */
export interface KubernetesNodeDrainArgs {
    /**
     * Whether to evict pods that use an emptyDir volume, whose data is lost once the pod is deleted. Mirrors `kubectl drain --delete-emptydir-data`.
     */
    deleteEmptyDirData?: pulumi.Input<boolean | undefined>;
    /**
     * Whether to delete pods directly instead of going through the eviction API, which ignores any configured PodDisruptionBudget. Mirrors `kubectl drain --disable-eviction`.
     */
    disableEviction?: pulumi.Input<boolean | undefined>;
    /**
     * Identifier of the Portainer Kubernetes environment the node belongs to.
     */
    environmentId: pulumi.Input<number>;
    /**
     * Whether to allow deletion of standalone pods that are not managed by a controller. Such pods are lost for good, so the drain refuses them unless this is enabled. Mirrors `kubectl drain --force`.
     */
    force?: pulumi.Input<boolean | undefined>;
    /**
     * Termination grace period in seconds to apply to every evicted pod. `-1` keeps each pod's own grace period. Mirrors `kubectl drain --grace-period`.
     */
    gracePeriodSeconds?: pulumi.Input<number | undefined>;
    /**
     * Whether to skip DaemonSet-managed pods, which would otherwise block the drain because their controller recreates them immediately. Mirrors `kubectl drain --ignore-daemonsets`.
     */
    ignoreDaemonSets?: pulumi.Input<boolean | undefined>;
    kubernetesNodeDrainId?: pulumi.Input<string | undefined>;
    /**
     * Name of the Kubernetes node to drain.
     */
    nodeName: pulumi.Input<string>;
    /**
     * Overall time in seconds to wait for the drain to complete. Mirrors `kubectl drain --timeout`.
     */
    timeoutSeconds?: pulumi.Input<number | undefined>;
}
