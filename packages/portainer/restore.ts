// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

export class Restore extends pulumi.CustomResource {
    /**
     * Get an existing Restore resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: RestoreState, opts?: pulumi.CustomResourceOptions): Restore {
        return new Restore(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'portainer:index/restore:Restore';

    /**
     * Returns true if the given object is an instance of Restore.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Restore {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === Restore.__pulumiType;
    }

    /**
     * Base64-encoded content of the backup archive, typically `filebase64("portainer-backup.tar.gz")`. A backup contains every credential Portainer holds, so it is stored in state as a sensitive value.
     */
    declare public readonly fileContentBase64: pulumi.Output<string>;
    /**
     * Name reported to Portainer for the uploaded archive.
     */
    declare public readonly fileName: pulumi.Output<string | undefined>;
    /**
     * Password the backup was encrypted with. Leave unset for an unencrypted backup.
     */
    declare public readonly password: pulumi.Output<string | undefined>;
    declare public readonly restoreId: pulumi.Output<string>;
    /**
     * Value sent as the `X-Setup-Token` header. Portainer requires it when the instance has not been initialised yet, which is the usual state for a restore.
     */
    declare public readonly setupToken: pulumi.Output<string | undefined>;

    /**
     * Create a Restore resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: RestoreArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: RestoreArgs | RestoreState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as RestoreState | undefined;
            resourceInputs["fileContentBase64"] = state?.fileContentBase64;
            resourceInputs["fileName"] = state?.fileName;
            resourceInputs["password"] = state?.password;
            resourceInputs["restoreId"] = state?.restoreId;
            resourceInputs["setupToken"] = state?.setupToken;
        } else {
            const args = argsOrState as RestoreArgs | undefined;
            if (args?.fileContentBase64 === undefined && !opts.urn) {
                throw new Error("Missing required property 'fileContentBase64'");
            }
            resourceInputs["fileContentBase64"] = args?.fileContentBase64 ? pulumi.secret(args.fileContentBase64) : undefined;
            resourceInputs["fileName"] = args?.fileName;
            resourceInputs["password"] = args?.password ? pulumi.secret(args.password) : undefined;
            resourceInputs["restoreId"] = args?.restoreId;
            resourceInputs["setupToken"] = args?.setupToken ? pulumi.secret(args.setupToken) : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["fileContentBase64", "password", "setupToken"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(Restore.__pulumiType, name, resourceInputs, opts, false /*dependency*/, utilities.getPackage());
    }
}

/**
 * Input properties used for looking up and filtering Restore resources.
 */
export interface RestoreState {
    /**
     * Base64-encoded content of the backup archive, typically `filebase64("portainer-backup.tar.gz")`. A backup contains every credential Portainer holds, so it is stored in state as a sensitive value.
     */
    fileContentBase64?: pulumi.Input<string | undefined>;
    /**
     * Name reported to Portainer for the uploaded archive.
     */
    fileName?: pulumi.Input<string | undefined>;
    /**
     * Password the backup was encrypted with. Leave unset for an unencrypted backup.
     */
    password?: pulumi.Input<string | undefined>;
    restoreId?: pulumi.Input<string | undefined>;
    /**
     * Value sent as the `X-Setup-Token` header. Portainer requires it when the instance has not been initialised yet, which is the usual state for a restore.
     */
    setupToken?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a Restore resource.
 */
export interface RestoreArgs {
    /**
     * Base64-encoded content of the backup archive, typically `filebase64("portainer-backup.tar.gz")`. A backup contains every credential Portainer holds, so it is stored in state as a sensitive value.
     */
    fileContentBase64: pulumi.Input<string>;
    /**
     * Name reported to Portainer for the uploaded archive.
     */
    fileName?: pulumi.Input<string | undefined>;
    /**
     * Password the backup was encrypted with. Leave unset for an unencrypted backup.
     */
    password?: pulumi.Input<string | undefined>;
    restoreId?: pulumi.Input<string | undefined>;
    /**
     * Value sent as the `X-Setup-Token` header. Portainer requires it when the instance has not been initialised yet, which is the usual state for a restore.
     */
    setupToken?: pulumi.Input<string | undefined>;
}
