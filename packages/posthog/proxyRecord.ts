// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

export class ProxyRecord extends pulumi.CustomResource {
    /**
     * Get an existing ProxyRecord resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ProxyRecordState, opts?: pulumi.CustomResourceOptions): ProxyRecord {
        return new ProxyRecord(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'posthog:index/proxyRecord:ProxyRecord';

    /**
     * Returns true if the given object is an instance of ProxyRecord.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ProxyRecord {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ProxyRecord.__pulumiType;
    }

    /**
     * Timestamp when the proxy record was created.
     */
    declare public /*out*/ readonly createdAt: pulumi.Output<string>;
    /**
     * Numeric identifier of the user who created the proxy record.
     */
    declare public /*out*/ readonly createdBy: pulumi.Output<number>;
    /**
     * The custom domain to provision in PostHog. Configured values are normalised to lowercase with no trailing dot, so `Example.COM.` and `example.com` plan as the same value.
     */
    declare public readonly domain: pulumi.Output<string>;
    /**
     * Additional status detail returned by PostHog, when present.
     */
    declare public /*out*/ readonly message: pulumi.Output<string>;
    /**
     * Organization for this resource. Overrides the provider-level organization_id. Accepts an organization UUID, an organization slug, or the literal `@current` (the authenticated user's organization); slugs and `@current` are resolved to a UUID for API calls while your original value is preserved in state.
     */
    declare public readonly organizationId: pulumi.Output<string>;
    /**
     * The current provisioning status reported by PostHog.
     */
    declare public /*out*/ readonly status: pulumi.Output<string>;
    /**
     * The PostHog-managed CNAME target that your DNS record must point to. Normalised without a trailing dot, ready to use as DNS record content. If your DNS provider requires the DNS-canonical dotted form (e.g. `hashicorp/dns`), append it yourself: `"${posthog_proxy_record.example.target_cname}."`.
     */
    declare public /*out*/ readonly targetCname: pulumi.Output<string>;
    /**
     * Timestamp when the proxy record was last updated.
     */
    declare public /*out*/ readonly updatedAt: pulumi.Output<string>;

    /**
     * Create a ProxyRecord resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ProxyRecordArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ProxyRecordArgs | ProxyRecordState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ProxyRecordState | undefined;
            resourceInputs["createdAt"] = state?.createdAt;
            resourceInputs["createdBy"] = state?.createdBy;
            resourceInputs["domain"] = state?.domain;
            resourceInputs["message"] = state?.message;
            resourceInputs["organizationId"] = state?.organizationId;
            resourceInputs["status"] = state?.status;
            resourceInputs["targetCname"] = state?.targetCname;
            resourceInputs["updatedAt"] = state?.updatedAt;
        } else {
            const args = argsOrState as ProxyRecordArgs | undefined;
            if (args?.domain === undefined && !opts.urn) {
                throw new Error("Missing required property 'domain'");
            }
            resourceInputs["domain"] = args?.domain;
            resourceInputs["organizationId"] = args?.organizationId;
            resourceInputs["createdAt"] = undefined /*out*/;
            resourceInputs["createdBy"] = undefined /*out*/;
            resourceInputs["message"] = undefined /*out*/;
            resourceInputs["status"] = undefined /*out*/;
            resourceInputs["targetCname"] = undefined /*out*/;
            resourceInputs["updatedAt"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ProxyRecord.__pulumiType, name, resourceInputs, opts, false /*dependency*/, utilities.getPackage());
    }
}

/**
 * Input properties used for looking up and filtering ProxyRecord resources.
 */
export interface ProxyRecordState {
    /**
     * Timestamp when the proxy record was created.
     */
    createdAt?: pulumi.Input<string | undefined>;
    /**
     * Numeric identifier of the user who created the proxy record.
     */
    createdBy?: pulumi.Input<number | undefined>;
    /**
     * The custom domain to provision in PostHog. Configured values are normalised to lowercase with no trailing dot, so `Example.COM.` and `example.com` plan as the same value.
     */
    domain?: pulumi.Input<string | undefined>;
    /**
     * Additional status detail returned by PostHog, when present.
     */
    message?: pulumi.Input<string | undefined>;
    /**
     * Organization for this resource. Overrides the provider-level organization_id. Accepts an organization UUID, an organization slug, or the literal `@current` (the authenticated user's organization); slugs and `@current` are resolved to a UUID for API calls while your original value is preserved in state.
     */
    organizationId?: pulumi.Input<string | undefined>;
    /**
     * The current provisioning status reported by PostHog.
     */
    status?: pulumi.Input<string | undefined>;
    /**
     * The PostHog-managed CNAME target that your DNS record must point to. Normalised without a trailing dot, ready to use as DNS record content. If your DNS provider requires the DNS-canonical dotted form (e.g. `hashicorp/dns`), append it yourself: `"${posthog_proxy_record.example.target_cname}."`.
     */
    targetCname?: pulumi.Input<string | undefined>;
    /**
     * Timestamp when the proxy record was last updated.
     */
    updatedAt?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a ProxyRecord resource.
 */
export interface ProxyRecordArgs {
    /**
     * The custom domain to provision in PostHog. Configured values are normalised to lowercase with no trailing dot, so `Example.COM.` and `example.com` plan as the same value.
     */
    domain: pulumi.Input<string>;
    /**
     * Organization for this resource. Overrides the provider-level organization_id. Accepts an organization UUID, an organization slug, or the literal `@current` (the authenticated user's organization); slugs and `@current` are resolved to a UUID for API calls while your original value is preserved in state.
     */
    organizationId?: pulumi.Input<string | undefined>;
}
