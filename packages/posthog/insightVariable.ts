// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

export class InsightVariable extends pulumi.CustomResource {
    /**
     * Get an existing InsightVariable resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: InsightVariableState, opts?: pulumi.CustomResourceOptions): InsightVariable {
        return new InsightVariable(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'posthog:index/insightVariable:InsightVariable';

    /**
     * Returns true if the given object is an instance of InsightVariable.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is InsightVariable {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === InsightVariable.__pulumiType;
    }

    /**
     * Code-safe name generated from <span pulumi-lang-nodejs="`name`" pulumi-lang-dotnet="`Name`" pulumi-lang-go="`name`" pulumi-lang-python="`name`" pulumi-lang-yaml="`name`" pulumi-lang-java="`name`" pulumi-lang-hcl="`name`">`name`</span>, referenced in HogQL as `{variables.code_name}`. PostHog derives it once at creation and keeps it if you later rename the variable, so queries do not break on a rename. It must be unique within the project.
     */
    declare public /*out*/ readonly codeName: pulumi.Output<string>;
    /**
     * Timestamp when the SQL variable was created.
     */
    declare public /*out*/ readonly createdAt: pulumi.Output<string>;
    /**
     * JSON-encoded default value used when a query references the variable and no value is supplied. The value is a bare JSON scalar for most types, so wrap it: `jsonencode("prod")`, `jsonencode(30)`, `jsonencode(true)`. For `List` variables PostHog stores the default as a string, so encode it as one.
     */
    declare public readonly defaultValueJson: pulumi.Output<string | undefined>;
    /**
     * Human-readable name for the SQL variable, shown above the input.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * Project ID (environment) for this resource. Overrides the provider-level project_id.
     */
    declare public readonly projectId: pulumi.Output<string>;
    /**
     * Variable type, which controls how the value is rendered and substituted into HogQL: `String`, `Number`, `Boolean`, `List`, or `Date`.
     */
    declare public readonly type: pulumi.Output<string>;
    /**
     * JSON-encoded array of allowed values, for `List` variables only. PostHog stores every entry as a string, so encode them as strings: `jsonencode(["1", "2"])` rather than `jsonencode([1, 2])`. Otherwise the value PostHog returns will not match your configuration. Leave unset for other variable types.
     */
    declare public readonly valuesJson: pulumi.Output<string | undefined>;

    /**
     * Create a InsightVariable resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: InsightVariableArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: InsightVariableArgs | InsightVariableState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as InsightVariableState | undefined;
            resourceInputs["codeName"] = state?.codeName;
            resourceInputs["createdAt"] = state?.createdAt;
            resourceInputs["defaultValueJson"] = state?.defaultValueJson;
            resourceInputs["name"] = state?.name;
            resourceInputs["projectId"] = state?.projectId;
            resourceInputs["type"] = state?.type;
            resourceInputs["valuesJson"] = state?.valuesJson;
        } else {
            const args = argsOrState as InsightVariableArgs | undefined;
            if (args?.type === undefined && !opts.urn) {
                throw new Error("Missing required property 'type'");
            }
            resourceInputs["defaultValueJson"] = args?.defaultValueJson;
            resourceInputs["name"] = args?.name;
            resourceInputs["projectId"] = args?.projectId;
            resourceInputs["type"] = args?.type;
            resourceInputs["valuesJson"] = args?.valuesJson;
            resourceInputs["codeName"] = undefined /*out*/;
            resourceInputs["createdAt"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(InsightVariable.__pulumiType, name, resourceInputs, opts, false /*dependency*/, utilities.getPackage());
    }
}

/**
 * Input properties used for looking up and filtering InsightVariable resources.
 */
export interface InsightVariableState {
    /**
     * Code-safe name generated from <span pulumi-lang-nodejs="`name`" pulumi-lang-dotnet="`Name`" pulumi-lang-go="`name`" pulumi-lang-python="`name`" pulumi-lang-yaml="`name`" pulumi-lang-java="`name`" pulumi-lang-hcl="`name`">`name`</span>, referenced in HogQL as `{variables.code_name}`. PostHog derives it once at creation and keeps it if you later rename the variable, so queries do not break on a rename. It must be unique within the project.
     */
    codeName?: pulumi.Input<string | undefined>;
    /**
     * Timestamp when the SQL variable was created.
     */
    createdAt?: pulumi.Input<string | undefined>;
    /**
     * JSON-encoded default value used when a query references the variable and no value is supplied. The value is a bare JSON scalar for most types, so wrap it: `jsonencode("prod")`, `jsonencode(30)`, `jsonencode(true)`. For `List` variables PostHog stores the default as a string, so encode it as one.
     */
    defaultValueJson?: pulumi.Input<string | undefined>;
    /**
     * Human-readable name for the SQL variable, shown above the input.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * Project ID (environment) for this resource. Overrides the provider-level project_id.
     */
    projectId?: pulumi.Input<string | undefined>;
    /**
     * Variable type, which controls how the value is rendered and substituted into HogQL: `String`, `Number`, `Boolean`, `List`, or `Date`.
     */
    type?: pulumi.Input<string | undefined>;
    /**
     * JSON-encoded array of allowed values, for `List` variables only. PostHog stores every entry as a string, so encode them as strings: `jsonencode(["1", "2"])` rather than `jsonencode([1, 2])`. Otherwise the value PostHog returns will not match your configuration. Leave unset for other variable types.
     */
    valuesJson?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a InsightVariable resource.
 */
export interface InsightVariableArgs {
    /**
     * JSON-encoded default value used when a query references the variable and no value is supplied. The value is a bare JSON scalar for most types, so wrap it: `jsonencode("prod")`, `jsonencode(30)`, `jsonencode(true)`. For `List` variables PostHog stores the default as a string, so encode it as one.
     */
    defaultValueJson?: pulumi.Input<string | undefined>;
    /**
     * Human-readable name for the SQL variable, shown above the input.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * Project ID (environment) for this resource. Overrides the provider-level project_id.
     */
    projectId?: pulumi.Input<string | undefined>;
    /**
     * Variable type, which controls how the value is rendered and substituted into HogQL: `String`, `Number`, `Boolean`, `List`, or `Date`.
     */
    type: pulumi.Input<string>;
    /**
     * JSON-encoded array of allowed values, for `List` variables only. PostHog stores every entry as a string, so encode them as strings: `jsonencode(["1", "2"])` rather than `jsonencode([1, 2])`. Otherwise the value PostHog returns will not match your configuration. Leave unset for other variable types.
     */
    valuesJson?: pulumi.Input<string | undefined>;
}
